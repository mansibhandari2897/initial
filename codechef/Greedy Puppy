#include<iostream>
using namespace std;
int main()
{
    int t,n,k;
    cin>>t;
    while(t>0)
    {
       cin>>n>>k;int maxi=0,r;
       for(int i=1;i<=k;i++)
       {
           r=n%i;
           if(r>maxi)
            maxi=r;
       }
       t--;cout<<maxi<<endl;
    }
}
