#include<iostream>
#include<iomanip>
using namespace std;
int main()
{
    int x;double y;
    cout<<fixed;
    cout<<setprecision(2);
    cin>>x>>y;
    if(x<=(y-0.50)&& x%5==0)
        cout<<(y-x-0.50);
    else
        cout<<(y);
}
